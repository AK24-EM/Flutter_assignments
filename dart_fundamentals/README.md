# 📘 Dart Fundamentals

A comprehensive collection of **core Dart language concepts** from basic syntax to advanced topics.

---

## 📁 Directory Structure

```
dart_fundamentals/
├── 01_variables.dart              # Variables, Data Types & Runes
├── 02_collections.dart            # Lists, Sets, Maps & Type Conversions
├── 03_operators.dart              # Arithmetic, Logical & Cascade Operators
├── 04_control_flow.dart           # If/Else, Loops & Switch-Case
├── 05_functions.dart              # Parameters, Arrow Functions & Closures
├── 06_null_safety.dart            # Nullable Types & Null-aware Operators
├── 07_advanced_control_flow.dart  # Labeled Breaks, Iterable Operations
├── 08_advanced_functions.dart     # Higher-Order Functions & Typedefs
├── 09_advanced_null_safety.dart   # Late Final, Generics Null Safety
├── file.dart                      # Dart File I/O Operations
├── main.dart                      # Flutter Demo Entry Point
└── README.md                      # Documentation
```

---

## 📚 Topics Covered

### Part 1: Fundamentals
1. **`01_variables.dart`**: `var`, `Object`, `dynamic`, `final` vs `const`, primitive data types (`int`, `double`, `String`, `bool`), string interpolation, runes.
2. **`02_collections.dart`**: `List<T>`, `Set<T>`, `Map<K, V>`, indexing, deduplication, type conversions (`int.parse`, `toStringAsFixed`).
3. **`03_operators.dart`**: Arithmetic, relational, equality, logical, ternary (`? :`), null-aware (`??`), cascade (`..`).
4. **`04_control_flow.dart`**: `if`/`else if`/`else`, `for`, `while`, `do-while`, `switch`/`case`, `break`, `continue`.
5. **`05_functions.dart`**: Positional and named parameters, default values, arrow syntax (`=>`), first-class functions, lexical closures.
6. **`06_null_safety.dart`**: Nullable (`?`), non-nullable, null assertion (`!`), null-coalescing (`??`), `late` variables.

### Part 2: Advanced Topics
7. **`07_advanced_control_flow.dart`**: Labeled loops, collection methods (`.where()`, `.map()`, `.forEach()`).
8. **`08_advanced_functions.dart`**: Higher-order functions, callbacks, `typedef` signatures, recursion.
9. **`09_advanced_null_safety.dart`**: Nullable generics, `late final` initialization, defensive null checks.

---

## 🏃 How to Run

Execute any script directly using Dart CLI:

```bash
dart run dart_fundamentals/01_variables.dart
dart run dart_fundamentals/02_collections.dart
dart run dart_fundamentals/05_functions.dart
# ...and so on
```
